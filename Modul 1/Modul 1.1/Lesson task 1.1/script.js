/**
 * This will add together value of a plus the value of b. 
 * @param {number} a This is the value of a
 * @param {number} b This is the value of b
 * @returns {number} The total value of a plus b is returned
 * @example
 * ```js
 * //Use this function to add together
 * //value of a plus value of b
 * const result = addNumbers(4, 6);
 * //Returns the total value of a and b
 * //10
 * ```
 */

function addNumbers(a, b) {
    const result = a + b;
    return result;
  }

  //addNumbers 
  //to see what i wrote in JSdoc